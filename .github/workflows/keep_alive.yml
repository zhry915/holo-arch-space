name: Keep Hugging Face Space Alive
于:
  schedule:
    - cron: "0 */24 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space
        run: |
          curl -I https://zhry-holo-arch-engine.hf.space
