name: Keep Hugging Face Space Alive
on:
  schedule:
    - cron: "0 */24 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space
        run: |
          curl -I https://huggingface.co/spaces/zhry/Holo-Arch-Engine
